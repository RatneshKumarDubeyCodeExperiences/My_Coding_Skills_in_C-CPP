#include <stdio.h>

int main(void) {
	// your code goes here
	int a;
	scanf("%d",&a);
	for(int i=0;i<a;i++)
	{
	    int x=0,y=0;
	    scanf("%d%d",&x,&y);
	    if(x<y)
	    printf("FIRST\n");
	    else if(y<x)
	    printf("SECOND\n");
	    else if(x==y)
	    printf("ANY\n");
	}
	return 0;
}
